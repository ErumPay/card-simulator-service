DB_URL=jdbc:mysql://mysql:3306/simulator_db
DB_USERNAME=root
DB_PASSWORD=1234

# 시뮬레이터 시드 사용자/카드 데이터는 seed.csv 에서 주입된다 (SimulatorDataSeeder).
# seed.csv 는 Dockerfile 의 COPY 로 이미지(/app/seed.csv)에 포함되므로 별도 마운트가 필요 없다.
# (경로를 바꾸려면 SIMULATOR_SEED_CSV_PATH 로 오버라이드 가능, 기본값은 application.yml 의 "seed.csv")
